using UnityEngine;
using TMPro;
public class Player : MonoBehaviour
{
    /*
    public TMP_Text moneyText;
    public float playerMoney = 100f;

    // Method to add money
    public void AddMoney(float amount)
    {
        playerMoney += amount;
        UpdateMoneyUI();
    }

    // Method to subtract money
    public void SubtractMoney(float amount)
    {
        if (playerMoney >= amount)
        {
            playerMoney -= amount;
            UpdateMoneyUI();
        }
        else
        {
            Debug.Log("Not enough money to subtract!");
        }
    }

    // Method to update the money display (you can modify this to fit your UI)
    private void UpdateMoneyUI()
    {
        // Find the moneyText object and update it (ensure it exists)
        TMP_Text moneyText = FindObjectOfType<ItemSelection>().moneyText; // or use a reference if available
        if (moneyText != null)
        {
            moneyText.text = "Money: $" + playerMoney.ToString("F2");
        }
    }*/
}
